# F06 — declared MIME controls active inline blob delivery

Recommend, high confidence; owner C04, worker F1 ../workers/C04-BLOBS.md. Fixed snapshot 3ee0621. Independently reopened attachment upload producer, protocol upload handler, normalize_mime, blob schema/writer/route, ThreadMessages attachment click, and complete router_from_state. Exact worker propagation query repeated: 8 matches.

Authenticated upload accepts declared image/svg+xml and opaque SVG bytes. blob_route.rs113–148 returns those bytes with Content-Type image/svg+xml and Content-Disposition inline because policy is mime.starts_with("image/"). ThreadMessages.tsx51 opens the signed route as a top-level document. router_from_state lib.rs740–768 serves app and blob routes together and adds no response CSP/sandbox/nosniff policy; the app's own HTML meta policy does not protect the separate response. Source therefore establishes active-document inline delivery on the application origin. Browser execution/exploit behavior has not been tested by this audit; do not claim an executed exploit.

Small target: retain arbitrary declared MIME as metadata but make inline policy a closed exact raster allowlist; SVG/unknown image subtypes download, add nosniff (and consider restrictive response CSP as defense in depth). No persisted disposition flag, DB/wire change, general attachment redesign or SVG sanitizer. Do not promise an allowlist validates payload bytes. Parent should verify browser policy and a real SVG attachment-open regression in an isolated fixture; preserve normal supported raster inline behavior, arbitrary downloads and signed URL authorization. Scope blob_route.rs plus meaningful route/browser tests; optional presentation adjustment only if needed.

Independent materiality qualification: include nosniff in the response contract. A closed MIME allowlist selects disposition; it does not validate actual file bytes. Keep browser behavior verification separate from route/header assertions.
