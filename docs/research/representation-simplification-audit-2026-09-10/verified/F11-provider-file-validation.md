# F11 — editable provider config bypasses required-field validation

Recommend; high confidence, medium priority. Owner C13, worker C13-01 ../workers/C13-CONFIG.md. Independently reopened ConfigStore::providers, add/update nonempty validation, boot provider resolution, selection seeding and documented editable-file contract. Exact propagation query repeated: 32 matches.

hirsel.toml is explicitly a supported Owner/Agent input and hot-reload surface (docs/hirsel-config.md3). Docs43–45 require base_url/default_model and promise malformed entries are warned and ignored. ConfigStore::providers instead accepts whitespace/empty base_url and defaults absent default_model to empty. With a key, boot treats the row as a usable OpenAI-compatible plan; selection offers it then fails to seed an empty model. This reachable file-input path bypasses the same invariants enforced by ordinary add/update commands, and prevents the documented omission/fallback behavior.

Target: one local provider-entry parser/validator shared with equivalent writer rules, requiring trimmed nonempty base_url/default_model before a StoredProvider is exposed. Preserve arbitrary supported endpoint syntax, existing label fallback and empty-key handling; log only provider identity and invalid field, never values/secrets. Keep TOML/wire/schema shape unchanged. Scope provider_store.rs and a small shared validator if needed, plus raw-TOML read/boot fallback fixtures for missing/empty/whitespace fields and a valid entry. No generalized config framework or union cutover. Audit read no live config and ran no tests.
