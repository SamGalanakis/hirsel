# F09 — accepted malformed regex monitor silently never wakes

Recommend; high confidence, medium priority. Authoritative finding owner C12 tool contract/parser; C08 owns storage/monitor consumer changes, not a second finding. Worker ../workers/C12-LASH-RUNTIME.md. Fixed source 3ee0621. Independently reopened tool_defs.rs304–328, executor parse_monitor_wake_on, scoped tool call, storage validation/create, current monitor DDL and monitor_should_wake. Exact worker queries repeated: 17 and 47 matches.

The agent-visible create schema accepts wake_on=regex and pattern="[". Storage validate_monitor_record only requires a nonempty pattern, so creation succeeds and persists a running monitor. Every actual tick calls Regex::new("[") and unwrap_or(false), silently suppressing all wakes. This is an ordinary invalid tool argument presented as successful monitor installation, not invented malformed server output. A pattern paired with changed/exit_* is also accepted and ignored; that supporting representational mismatch is not the main impact.

Target: one MonitorCondition with Changed, ExitZero, ExitNonzero, Regex(validated pattern), created before persistence. Expose the corresponding tagged/conditional tool schema so regex requires its pattern and other variants cannot silently accept one; keep existing serialized field names if cleanly supported. Validate actual regex syntax at admission, not by interpreting compilation failure as nonmatch every tick. Durable current.sql should enforce the variant/pattern relationship; syntax validation remains owned by host construction/read validation. Avoid storing a second compiled/syntax-valid boolean. Preserve current wake timing, labels and no-auto-replay policy.

Small coherent scope: tool input/output schemas and parser, scoped call, MonitorRecord creation/storage decoding/DDL and runner condition match; no generic runtime redesign or new wake timestamps. A narrower initial guard is enough to prove the behavior regression, but final representation should not keep invalid optional-field combinations as the public model. Tests should reject missing/empty/malformed regex before any monitor row/process exists, reject irrelevant patterns, accept all valid variants and preserve matching/nonmatching behavior. Strict schema cutover requires root coordination with other schema fixes. No audit tests/source edits.

Independent materiality qualification: tool JSON maps its conditional wake_on/pattern pair to host Changed | ExitZero | ExitNonzero | Regex(ValidatedPattern); SQL retains discriminator/pattern columns with an explicit NULL-safe relationship check; storage reads reconstruct the validated condition and runner matches it. Four of eight kind-by-presence classes are meaningful, with regex syntax an additional invariant. Preserve exact nonempty pattern text, including literal spaces. Whether compiled regex is retained process-locally or compiled per match is an explicit implementation choice; no regex performance improvement or bounded-cost claim was established.
