# Thread workspace

Mode: Operate. Existing Inter, mint/slate palette, cube mark and light/dark themes remain authoritative. The Owner reads and replies to one Thread, opens artifacts, and moves between independent conversations without losing drafts or attachments.

## Direction contract

THESIS: A narrow global icon rail gives the conversation its space. A focused Thread becomes an inset work surface; an unselected overview invites an explicit choice before composing.

OWN-WORLD: Incumbent Inter, quiet mint selection, slate content, fine borders and the original cube. Consistent licensed Lucide line icons carry utilities, with accessible names, tooltips and 44px touch targets.

STORY: The Owner sees which Thread is addressed through the selected rail icon, frame and back affordance. The branch icon opens the Thread drawer; selection closes it and restores the conversation.

FIRST VIEWPORT: A 56px rail contains Thread overview, Threads, New thread, global artifacts and activity, with Settings at the bottom. The overview offers explicit creation/selection without a composer. A focused Thread occupies the remaining inset framed surface, with a compact back/name/Conversation/Artifacts/actions strip. Its composer stays at the bottom; detailed execution remains disclosed. Phone keeps the rail and a tighter frame, with a modal drawer and full-screen artifact preview.

FORM: Approved option B nested Threads and pinning, retaining option A’s icon rail and focus frame, direction key compact-thread-context. User approved the nested Thread model and caller-relative coordination. Existing icons remain. The two panels in the proposal describe states of ONE app. GLOBAL/IN A THREAD are comparison-only labels. Duplicate global top utilities are omitted as approved.

Signature interaction: the selected branch icon connects visually to the focused frame; the overview removes the addressed composer. Thread rows show authoritative working duration, queue count and last turn outcome separately from attention, unread and explicit settlement. Row, context-strip and palette actions share one definition. The drawer preserves canvas colors with a transparent backdrop and retains native modal focus. The drawer header holds compact filter and search buttons; filter choices stay disclosed. One browse/create intent controls its initial focus. The rail quietly signals visible Threads needing the Owner, including when snoozes expire. The existing palette finds named Threads and exact #references without changing the draft until selection. Drawers and previews return keyboard focus. Motion remains short control feedback, respecting reduced motion.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

Current finish requirements: exact per-assistant execution association and page-aware chronology, safe Markdown file previews with source downloads, Escape across all artifact formats, correctly sized restored phone drafts, Thread-first palette search, visible compact numeric identity, selected filter caption, and AA light attention contrast. History resets preserve recoverable text while invalidating caches and pending operations.

Hierarchy: immutable parent IDs form the human tree. Pin shortcuts use stable pin time order, independent of lifecycle. Show exact breadcrumbs and concise child reports with artifact references; preserve focused child conversations, current execution chronology and draft isolation. No separate Project/Namespace type, reserved ID0, implicit recipient, new global agent inventory or icon replacement.
